using System;
using System.Collections.Generic;
using System.Data;
using TrackerSQL.Classes.Poco;

namespace TrackerSQL.Classes.Sql
{
    /// <summary>
    /// Repository for RecurranceType entity
    /// Auto-generated: 2026-06-02 13:35:41
    /// </summary>
    public class RecurranceTypeRepository : RepositoryBase<RecurranceType>
    {
        protected override string TableName => "RecurranceTypeTbl";
        protected override string KeyColumn => "RecurringTypeID";

        /// <summary>
        /// Maps a DataReader row to a RecurranceType object
        /// </summary>
        protected override RecurranceType Map(IDataReader reader)
        {
            return DbMapper.Map<RecurranceType>(reader);
        }

        /// <summary>
        /// Gets all RecurranceType records
        /// </summary>
        /// <param name="sortColumn">Column to sort by (optional)</param>
        public List<RecurranceType> GetAll(string sortColumn = null)
        {
            string sql = `$"SELECT * FROM {TableName}"`;
            if (!string.IsNullOrEmpty(sortColumn))
            {
                sql += `$" ORDER BY {sortColumn}"`;
            }

            return ExecuteQuery(sql);
        }

        /// <summary>
        /// Gets a single RecurranceType by ID
        /// </summary>
        public RecurranceType GetById(int id)
        {
            string sql = `$"SELECT * FROM {TableName} WHERE {KeyColumn} = @Id"`;
            var parameters = new List<DBParameter>
            {
                new DBParameter { DataValue = id, DataDbType = DbType.Int32, ParamName = "@Id" }
            };

            return ExecuteQuerySingle(sql, parameters);
        }

        /// <summary>
        /// Inserts a new RecurranceType record
        /// </summary>
        public int Insert(RecurranceType entity)
        {
            if (entity == null) throw new ArgumentNullException(nameof(entity));

            // TODO: Customize INSERT statement based on POCO properties
            // This is a template - adjust column names as needed
            string sql = @"
                INSERT INTO RecurranceTypeTbl (
                    -- Add column names here
                )
                VALUES (
                    -- Add parameter names here (@Param1, @Param2, etc.)
                );
                SELECT CAST(SCOPE_IDENTITY() AS INT);";

            var parameters = new List<DBParameter>
            {
                // TODO: Add parameters based on POCO properties
                // Example:
                // new DBParameter { DataValue = entity.PropertyName, DataDbType = DbType.String, ParamName = "@PropertyName" }
            };

            return ExecuteScalar<int>(sql, parameters);
        }

        /// <summary>
        /// Updates an existing RecurranceType record
        /// </summary>
        public bool Update(RecurranceType entity)
        {
            if (entity == null) throw new ArgumentNullException(nameof(entity));

            // TODO: Customize UPDATE statement based on POCO properties
            string sql = @"
                UPDATE RecurranceTypeTbl
                SET 
                    -- Add column = @Param pairs here
                WHERE RecurringTypeID = @Id";

            var parameters = new List<DBParameter>
            {
                // TODO: Add parameters based on POCO properties
                // Don't forget to add the ID parameter
            };

            int result = ExecuteNonQuery(sql, parameters);
            return result > 0;
        }

        /// <summary>
        /// Deletes a RecurranceType record by ID
        /// </summary>
        public bool Delete(int id)
        {
            string sql = `$"DELETE FROM {TableName} WHERE {KeyColumn} = @Id"`;
            var parameters = new List<DBParameter>
            {
                new DBParameter { DataValue = id, DataDbType = DbType.Int32, ParamName = "@Id" }
            };

            int result = ExecuteNonQuery(sql, parameters);
            return result > 0;
        }

        // TODO: Add custom methods from legacy RecurranceTypeTbl.cs here
        // Example:
        // public int GetCustomCount()
        // {
        //     string sql = "SELECT COUNT(*) FROM RecurranceTypeTbl WHERE CustomCondition";
        //     return ExecuteScalar<int>(sql);
        // }
    }
}
