# LEGACY — do not use

Typed DataSets / TableAdapters under `DataSets/` are **Access-era leftovers**.

**Active data access:** `Repositories/` + `TrackerSQLDb` (see `Documentation/HARD_PROJECT_RULES.md`).

Nothing in the current TrackerSQL build compiles or publishes these files. Legacy pages that still mentioned them (`ClientList`, `OrdersEdit`, `OrderSheet`, `NewOrder`) are also excluded from the project.

Keep on disk for historical reference only.
